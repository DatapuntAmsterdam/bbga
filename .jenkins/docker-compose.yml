database:
  image: admin.datapunt.amsterdam.nl:5000/atlas/postgres
  environment:
    POSTGRES_PASSWORD: insecure
    POSTGRES_USER: bbga

importer:
  build: ../web
  links:
    - database:database
  volumes:
    - /mnt/data/bbga:/app/diva
#    - ../data:/app/data
  environment:
    DB_NAME: bbga
    DB_USER: bbga
    DB_PASSWORD: insecure
  command: >
    bash -c "/app/docker-wait.sh \
            && bash import_data.sh "

db-backup:
  image: admin.datapunt.amsterdam.nl:5000/atlas/postgres
  links:
    - database:db
  volumes:
    - ./backups:/tmp/backups
  command: >
    bash -c "echo db:5432:bbga:bbga:insecure > ~/.pgpass \
            && chmod 600 ~/.pgpass \
            && pg_dump --clean \
                        -Fc \
                        -t bbga* \
                        -T django*  \
                        -T auth*    \
                        -U bbga \
                        -h db -p 5432 \
                        bbga > /tmp/backups/database.dump"
